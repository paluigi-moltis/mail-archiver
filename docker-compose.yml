services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: mail_archive
      POSTGRES_PASSWORD: mail_archive_dev
      POSTGRES_DB: mail_archive
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
